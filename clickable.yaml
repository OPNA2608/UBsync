clickable_minimum_required: "8.0.0"
builder: "cmake"

dependencies_target:
- "libaccounts-qt5-dev"
- "libsignon-qt5-dev"

build_args:
- "-DCLICK_MODE:BOOL=ON"

libraries:
  update-po-files:
    builder: custom
    build: cd ${ROOT} && po/update-translations.sh
